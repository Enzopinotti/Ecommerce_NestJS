name: Quality

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - modernize/2026-nest-quality
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout exact commit
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup pinned Node runtime
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Verify toolchain identity
        shell: bash
        run: |
          set -euo pipefail
          expected_node="$(tr -d '\r\n' < .nvmrc)"
          actual_node="$(node --version)"
          expected_npm="$(node -p "require('./package.json').packageManager.split('@')[1]")"
          actual_npm="$(npm --version)"

          printf 'node=%s\n' "$actual_node"
          printf 'npm=%s\n' "$actual_npm"
          printf 'sha=%s\n' "$GITHUB_SHA"

          test "$actual_node" = "$expected_node"
          test "$actual_npm" = "$expected_npm"

      - name: Install from lockfile
        run: npm ci

      - name: Blocking B1 quality contract
        id: quality
        run: npm run quality

      - name: Full-project typecheck debt inventory
        id: typecheck_all_debt
        if: always()
        continue-on-error: true
        run: npm run typecheck:all

      - name: Formatting debt inventory
        id: format_debt
        if: always()
        continue-on-error: true
        run: npm run format:check

      - name: Production dependency audit inventory
        id: audit_debt
        if: always()
        continue-on-error: true
        run: npm audit --omit=dev --audit-level=high

      - name: Write quality evidence summary
        if: always()
        shell: bash
        run: |
          {
            echo "# B1 quality evidence"
            echo
            echo "- SHA: \`${GITHUB_SHA}\`"
            echo "- Node: \`$(node --version 2>/dev/null || echo unavailable)\`"
            echo "- npm: \`$(npm --version 2>/dev/null || echo unavailable)\`"
            echo "- blocking quality: \`${{ steps.quality.outcome }}\`"
            echo "- full-project typecheck debt: \`${{ steps.typecheck_all_debt.outcome }}\`"
            echo "- format debt inventory: \`${{ steps.format_debt.outcome }}\`"
            echo "- production audit inventory: \`${{ steps.audit_debt.outcome }}\`"
            echo
            echo "The blocking B1 contract validates deployable TypeScript and ratchets the B0 lint/unit baselines instead of pretending historical debt is already fixed."
            echo "The known e2e type error, formatting debt and dependency advisories remain explicit non-blocking inventories until their dedicated modernization blocks reduce them."
          } | tee quality-summary.md
          cat quality-summary.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload quality evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: b1-quality-${{ github.sha }}
          retention-days: 30
          if-no-files-found: error
          path: quality-summary.md
